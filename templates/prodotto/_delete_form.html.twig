<form method="post" action="{{ path('app_prodotto_delete', {'id': prodotto.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prodotto.id) }}">
    <button class="btn">Delete</button>
</form>
